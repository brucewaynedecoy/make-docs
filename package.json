{
  "name": "starter-docs-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo root for starter-docs. The publishable CLI lives at packages/cli; the template lives at packages/docs/template.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w starter-docs",
    "dev": "npm run dev -w starter-docs",
    "test": "npm test -w starter-docs",
    "validate:defaults": "npm run validate:defaults -w starter-docs",
    "smoke:pack": "node scripts/smoke-pack.mjs"
  }
}
