interface:
  display_name: "Work On Wave"
  short_description: "Work on docs/work waves through implementation, validation, closeout, and phase commits"
  default_prompt: "Use $work-on-wave to work on the target docs/work wave or phase end to end with checkpoints and phase gates."
policy:
  allow_implicit_invocation: true
