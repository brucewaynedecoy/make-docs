# Legacy Agent Session Guides

This directory contains legacy point-in-time session breadcrumbs. Do not create new history records here.

New agent history records belong under `docs/.assets/history/`.

## Agent Instructions

- For new history records, read `docs/.references/agent-guide-contract.md`, `docs/.templates/agent-guide.md`, and the router in `docs/.assets/history/`.
- Edit legacy records only to correct factual errors or broken links.
- Do not append new session work to a prior session's summary.
