# Phase 04: Follow-On Implementation Backlog

## Purpose

Generate the follow-on backlog for modular TypeScript operation domains and the required TypeScript MCP runtime.

## Tasks

- [x] t1: Generate a W10 R8 plan.
- [x] t2: Generate a paired W10 R8 work backlog.
- [x] t3: Require modular operation domains that mirror CLI/MCP command domains.
- [x] t4: Require remote package-runner validation for `npx`, `pnpm dlx`, and `bunx` / `bun x`.
- [x] t5: Preserve existing `make-docs operations ...` behavior during refactor.

## Implementation Notes

- Generated W10 R8 as an implementation follow-on instead of adding code work to W10 R7.

## Acceptance Criteria

- The W10 R8 backlog is executable without reopening the W10 R7 architecture decision.
