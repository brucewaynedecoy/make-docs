# Requirements and Register Reconciliation

## Objective

Land the PRD and risk-register updates needed before CLI/MCP implementation starts.

## Tasks

- Add PRD 25 to the active PRD index and source anchors.
- Annotate affected PRDs with TypeScript installer ownership, Rust MCP ownership, and MCP parity constraints.
- Update risk register entries for command docs, package docs, shared skill/plugin routing, no-scripts migration, and parity validation.
- Keep unresolved remote skill, alternate manifest, and shared plugin install questions open.

## Acceptance Criteria

- PRD 25 is discoverable from the active PRD set.
- No affected PRD implies MCP can define a separate behavior model.
- Open risks preserve unresolved downstream decisions instead of over-closing them.
