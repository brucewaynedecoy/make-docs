---
title: "W18 R10 P5 Runtime-State Guidance and Verification"
kind: "history"
status: "completed"
date: "2026-07-01"
client: "Claude Code"
model: "Fable 5"
coordinate: "W18 R10 P5"
repo: "make-docs"
branch: "make-docs-v2"
summary: "Landed the template runtime-state guidance flip, the D11 verification suite, and the packed-CLI store assertions; completed the W18 R10 wave; ran the wave-completion UAT and closed R-023."
---

# W18 R10 P5 Runtime-State Guidance and Verification

## Changes

Implemented W18 R10 Phase 5 of [PRD 38](../../../prd/38-revise-global-store-and-project-state.md) — the runtime-state guidance refresh and the D11 verification pass — completing the final phase of the wave and, with it, the whole two-wave global-store effort's R10 lineage. The guidance flip was authored upstream-first per the maintainer dogfooding rule: [`packages/docs/template/.make-docs/AGENTS.md`](../../../../packages/docs/template/.make-docs/AGENTS.md) and [`CLAUDE.md`](../../../../packages/docs/template/.make-docs/CLAUDE.md) no longer name `.make-docs/runs/` as a runtime-state location — project `.make-docs/` retains `manifest.json`, `conflicts/`, and project config, while run-state and work-execution evidence live in the machine-level global store at `~/.make-docs/` — and the dogfood copies [`.make-docs/AGENTS.md`](../../../../.make-docs/AGENTS.md) and [`.make-docs/CLAUDE.md`](../../../../.make-docs/CLAUDE.md) are byte-identical to them. The t2 sweep found only the two routers needing reconciliation: the path-and-link-hygiene reference, the docs and assets routers, and an explicitly historical commit-convention example were verified accurate as-is. The new D11 suite [`packages/cli/tests/store-verification.test.ts`](../../../../packages/cli/tests/store-verification.test.ts) (6 tests, one `describe` block per requirement) proves R-TEST-1 by repo-snapshot equality — checkpoint evidence and Playbook run records written through the real seams leave the repository byte-for-byte untouched — R-TEST-2 move/clone survival with the identifier re-resolving and the stored `repoRoot` proven secondary, R-TEST-3 missing-and-corrupt-database degradation reported as recoverable operational-state loss, and R-TEST-4 multi-project prune scoping plus `removeGlobalStore` leaving repositories byte-identical, with deep legs referenced in the P1–P4 suites. [`scripts/smoke-pack.mjs`](../../../../scripts/smoke-pack.mjs) was extended to sandbox `MAKE_DOCS_HOME` for every packed-CLI invocation (also fixing a pre-existing leak into the real home directory), assert store bootstrap and no repository-path state writes after the bare install and in the npx/pnpm/bun runner smokes, assert packed-template router guidance parity against the dogfood copies, and end the run asserting no `.make-docs/runs/` exists under the target. A small consistency fix updated the `checkpoint` line in [`src/cli.ts`](../../../../packages/cli/src/cli.ts) operations help ("Record phase evidence in the machine-level global store") whose old text contradicted the landed P3 behavior. The W18 R7 boundary was left alone per R-SCOPE-1: the Playbook runner still writes `.make-docs/runs/playbooks/**`, and its relocation onto the `playbook_runs` seam is the gated W18 R7 work. All 8 boxes in [the Phase 5 backlog file](../../../work/2026-07-01-w18-r10-global-store-and-project-state/05-runtime-state-guidance-and-verification.md) are checked; the full suite passes 35 files / 610 tests, the build is green, and `node scripts/smoke-pack.mjs` exits 0. Wave-completion status: all five W18 R10 phases (P1 store bootstrap, P2 identity, P3 state model and evidence migration, P4 lifecycle and privacy, P5 guidance and verification) are implemented with history records; the open handoffs are W18 R7 (Playbook run-state relocation, R-TEST-5) and W18 R11 (command surface for `pruneProjectFromStore` and `removeGlobalStore`).

Developer-guide coverage was `update-existing` because [the docs assets and runtime state boundaries guide](../../library/developer/maintainer-docs-assets-and-runtime-state-boundaries.md) owns the runtime-state boundary story and its Future Coverage gate fired on Phase 5: the guide's checkpoint bullet now explains that the `tool-directory` runtime-state classification of `.make-docs/runs/**` stays correct both for unmigrated legacy files and for the Playbook runner's still-current writes pending W18 R7; a new boundary bullet records that the shipped `.make-docs` routers now match the behavior and names the twofold enforcement mechanism — the D11 suite at the module seams and the smoke-pack assertions at the packaged surface; the `MAKE_DOCS_HOME` isolation note records that `scripts/smoke-pack.mjs` now sandboxes every packed-CLI invocation; the store-touching maintainer-check row gained the D11 suite and the smoke-pack finish; and the Future Coverage gate narrowed from "Phase 5 plus the W18 R7/R11 consumers" to the W18 R7 and W18 R11 consumers only. User-guide coverage was `none` for both [the installation guide](../../library/user/getting-started-installing-make-docs.md) and [the lifecycle guide](../../library/user/cli-lifecycle-managing-installations.md): neither names `.make-docs/runs/`, their store story (bootstrap on first apply, `projectId` minting, uninstall disposition report, local-only privacy) landed at P1–P4 and still matches shipped behavior verbatim, and Phase 5's changes are agent-router guidance, maintainer smoke infrastructure, and operations help text — none of which alters a user-visible statement in either guide. PRD coverage was `risk-register-update`: the phase implemented existing PRD 38 requirements, and PRD 38's documentation-consequence language (the `.make-docs/` runtime-state guidance updated upstream then dogfooded) is now satisfied by the implementation itself, so no baseline change note was needed. In [the open questions and risk register](../../../prd/03-open-questions-and-risk-register.md), R-019 records the P5 landing and the wave completion but stays Open by its stated close bar — W18 R7 storage work has not yet built on the store and the R-TEST-5 no-repo-run-state assertion for Playbook runs remains outstanding — with its Follow-Up retargeted to the W18 R7 consumption alone since the store prerequisite is fully resolved; R-023 is Closed with a `**Resolution**` block, because its P4-recorded close bar required exactly the P5 verification pass that has now re-proven the mirror-subordination, no-repo-run-state, and evidence-integrity guards end to end at both the module seams and the packaged surface. Register headings, item numbers, and the fixed sections are unchanged, and the register-pinning consistency suite passes.

### Wave-Completion UAT

Manual end-user testing was judged worthwhile (`create`): the wave's user-observable story — store bootstrap, identity minting, store-only evidence, and the uninstall disposition — had been deferred to wave completion per user instruction and is exactly the flow a real consumer exercises. The scenario, run against the built CLI (`packages/cli/dist/index.js`, 1.0.0-rc.1, Node v24.10.0) in a session-scratchpad sandbox with an isolated `MAKE_DOCS_HOME` and a fresh `git init` demo repo, never touching the real home directory or this repo's installed instance:

| Step | Action | Expected | Observed |
| --- | --- | --- | --- |
| 1 | `make-docs --yes --target <demo-repo>` (fresh install) | Install completes; store bootstrapped at `$MAKE_DOCS_HOME` | Pass — installed 1.0.0-rc.1; `config.json`, `manifest.json`, `store.db` created; global manifest records `schemaVersion` 1, database `status: "created"` |
| 2 | Inspect project manifest and store | `projectId` minted in `.make-docs/manifest.json`; matching mirror row in `projects` with `root_path` as metadata; no `.make-docs/runs/` | Pass — `projectId` `c76eeb8f-…` present in both, no `runs/` directory |
| 3 | `operations checkpoint docs/work/<wave>/01-setup.md --validation-status passed --validation-command "npm test" --review-status waived --note …` against a minimal demo backlog | Evidence recorded with `evidenceSource: "global-store"`; no dropped genuine fields | Pass — JSON reports `evidenceSource: "global-store"`, validation/review/notes evidence, `droppedFields: []`, `legacyMigration.migrated: false` (no legacy file existed) |
| 4 | Verify storage location and readback | Evidence rows in `store.db` only; nothing under any repository path; `phase-gate` reads evidence back from the store | Pass — three `work_evidence` rows keyed by `projectId` + wave slug + phase path; no `.make-docs/runs/` and no `state.json` anywhere in the repo; `phase-gate` returned the evidence with `projectIdentity.status: "resolved"` and honest blockers for the unrecorded closeout/commit |
| 5 | `make-docs uninstall --yes --target <demo-repo>` | Disposition report printed: this project pruned, last-project guidance naming the store location; repository content intact | Pass — "Global store: pruned this project's operational state (project c76eeb8f-…)" plus "No registered projects remain… it can be safely deleted at <store root> (it holds only operational state, never repository content)"; the user-authored demo backlog survived, and all three store tables show zero rows with the store files retained |

Pass rule: every step's observed behavior matches the expected column with no store file, warning, or write appearing outside the sandboxed `MAKE_DOCS_HOME` and no run-state appearing under the repository. Result: pass, five of five steps.

Validation: jdocmunch was reindexed over the docs tree, `.make-docs/scripts/check_path_hygiene.py` reports zero errors, every relative link added this session resolves, `git diff --check` is clean, the register-pinning consistency suite passes (33 tests), and `diff -q` confirms both template/dogfood router pairs are byte-identical. The uncommitted W18 R10 P5 implementation changes were left in place for the orchestrator's commit, and the concurrent W18 R6 artifacts (`docs/work/2026-07-01-w18-r6-*`) were untouched. Remaining debt is the two recorded handoffs only: W18 R7 (run-state relocation and R-TEST-5, carried by R-019) and W18 R11 (lifecycle command surface, carried by R-024).

## Documentation

### Project

| Path | Description |
| --- | --- |
| [../../../prd/03-open-questions-and-risk-register.md](../../../prd/03-open-questions-and-risk-register.md) | Recorded the P5 landing and wave completion in R-019 (stays Open pending W18 R7 R-TEST-5) and closed R-023 with a Resolution block citing the end-to-end verification; headings, numbers, and fixed sections preserved. |
| [../../../work/2026-07-01-w18-r10-global-store-and-project-state/05-runtime-state-guidance-and-verification.md](../../../work/2026-07-01-w18-r10-global-store-and-project-state/05-runtime-state-guidance-and-verification.md) | Phase 5 tasks t1 through t8 marked complete by the implementation session. |
| [../../../../packages/docs/template/.make-docs/AGENTS.md](../../../../packages/docs/template/.make-docs/AGENTS.md) | Upstream runtime-state guidance flip authored by the implementation session: run-state and work-execution evidence live in the global store; `.make-docs/runs/` no longer named. |
| [../../../../.make-docs/AGENTS.md](../../../../.make-docs/AGENTS.md) | Dogfood copy of the guidance flip, byte-identical to the template (`CLAUDE.md` pair likewise). |

### Developer

| Path | Description |
| --- | --- |
| [../../library/developer/maintainer-docs-assets-and-runtime-state-boundaries.md](../../library/developer/maintainer-docs-assets-and-runtime-state-boundaries.md) | Recorded the guidance flip and its twofold enforcement (D11 suite, smoke-pack assertions), the W18 R7 Playbook-runner boundary, the smoke-pack `MAKE_DOCS_HOME` sandboxing, an extended store-touching maintainer check, and the Future Coverage gate narrowed to the W18 R7/R11 consumers only. |

### User

None this session.
